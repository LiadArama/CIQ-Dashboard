import React, {useState, useEffect} from 'react';

function AvgTimePerPage(){
    const [isLoading, setIsLoading] = useState(true);
    const [date, setDate] = useState([]);

    useEffect(()=>{

    })
}

